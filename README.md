# AgileActivity

The .venv folder isn't used (but I'm not removing it since I don't know if that'll break anything). The reason why
is because I used a virtual environment initially, but that didn't work out.

By extension, I don't know if the ```*project.toml*``` file is required either, but it's there just in case.